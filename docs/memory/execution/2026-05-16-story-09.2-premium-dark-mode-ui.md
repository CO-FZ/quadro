# Sessão Story 09.2 — Premium Dark Mode UI — 2026-05-16

**Status final:** done
**Story:** docs/sprints/09/story-09.2-premium-dark-mode-ui.md

## Plano original

Atualizar `app/globals.css` para dark mode premium: primary teal, glassmorphism, fonte Inter corrigida.

## Arquivos alterados

| Arquivo | Operação | Razão |
|---------|----------|-------|
| `app/globals.css` | atualizado | dark mode primary teal, glassmorphism vars, font-sans corrigido |
| `docs/sprints/09/story-09.2-premium-dark-mode-ui.md` | criado | arquivo de story ausente no repositório |

## Mudanças em globals.css

- Dark mode `--primary` alterado de `oklch(0.70 0.14 260)` (azul) para `oklch(0.72 0.17 195)` (teal)
- Background/card/border hues movidos para 220° para coerência teal
- Adicionados `--glass-bg: oklch(0.22 0.012 195 / 0.55)` e `--glass-border: oklch(0.55 0.08 195 / 0.25)`
- `--font-sans` corrigido: removido `var(--font-geist-sans)` (não carregado em layout.tsx) → `var(--font-inter), sans-serif`
- `@theme inline` mapeado com `--color-glass-bg` e `--color-glass-border`
- `@layer utilities` adicionado com `.glass` (backdrop-filter: blur(12px))

## Decisões pequenas tomadas

- `--font-geist-sans` era referência morta: `layout.tsx` importa apenas Inter via `next/font/google`. Variável `--font-geist-sans` nunca era definida — font-sans resolvia para sans-serif puro. Corrigido para Inter explícito.
- Glassmorphism vars usam `oklch(L C H / alpha)` syntax — sem fallback necessário pois Tailwind v4 já exige suporte a oklch.

## Verificação

- `pnpm typecheck` → 0 erros (CSS não verificado por tsc; sem impacto)
- Validação visual em browser: não realizada nesta sessão (browser subagent não disponível)

## Harness debt observada

- Story file (09.2) não existia antes desta sessão — criado retroativamente como parte da execução
- Execution log não escrito ao final da sessão original — corrigido retroativamente neste arquivo

## Pendências

- Nenhuma. Sprint 09 encerrada.
